# 꿈들 홈페이지 — 회원 데이터 보호 보안 조치 방안

이 문서는 꿈들 홈페이지의 **회원가입·회원 데이터 보호를 위한 구체적인 보안 조치**를 정리한 것입니다.
"코드에 이미 반영된 것", "SQL로 1회 실행할 것", "Supabase 화면에서 클릭으로 설정할 것",
"운영하면서 지킬 것"의 네 층으로 나누어, 순서대로 따라 하면 완료되도록 구성했습니다.

---

## 0. 지금 발생한 "가입 실패: Failed to fetch" — 원인과 해결

"Failed to fetch"는 비밀번호가 틀렸다는 뜻이 아니라, **브라우저가 데이터베이스 서버
(`gywbsiatubvvtnblvwdz.supabase.co`)에 아예 연결하지 못했다**는 뜻입니다.

가장 흔한 원인은 **Supabase 무료 프로젝트의 일시중지(Paused)** 입니다.
무료 요금제는 약 1주일간 아무 요청이 없으면 프로젝트가 자동으로 잠들며, 그동안 모든 요청이
이렇게 실패합니다. 해결 방법은 다음과 같습니다.

1. https://supabase.com/dashboard 에 로그인합니다.
2. 꿈들 프로젝트를 클릭합니다. **"Paused"** 표시가 있으면 **Restore project** 버튼을 누릅니다.
3. 1~2분 뒤 프로젝트가 깨어나면 홈페이지에서 다시 회원가입을 시도합니다. 데이터는 사라지지 않습니다.

프로젝트가 Paused가 아닌데도 계속 실패한다면 순서대로 확인합니다.

- 프로젝트가 **삭제**되지 않았는지 (삭제됐다면 새 프로젝트를 만들고 `SUPABASE_SETUP.md`를 처음부터 다시 진행)
- Project Settings → API 의 **Project URL**이 `assets/config.js`의 값과 정확히 같은지
- 학교·기관 네트워크라면 보안 장비가 `*.supabase.co`를 차단하는 경우가 있으니, 휴대폰 데이터(LTE/5G)로 접속해 비교해 봅니다.

> 재발 방지: 프로젝트가 다시 잠들지 않도록 **최소 주 1회 관리자 페이지(admin.html)에 접속**하거나,
> Supabase 대시보드에 접속하는 습관을 들이는 것이 가장 간단합니다.
> 방문자가 어느 정도 생기면 자연히 잠들지 않습니다.

또한 이번 수정으로, 같은 오류가 나면 홈페이지 화면에
**"서버에 연결하지 못했습니다 … 관리자에게 알려주세요"** 라는 한국어 안내가 뜨도록 바꾸었습니다.

---

## 1. 어떤 데이터가 어디에 저장되는가

| 데이터 | 저장 위치 | 내용 |
|---|---|---|
| 계정(이메일·비밀번호) | Supabase `auth.users` | 비밀번호는 **bcrypt 단방향 해시**로 저장 — 운영진도 원문을 볼 수 없음 |
| 회원 프로필 | `public.profiles` | 이름, 이메일, 연락처, 권한(member/admin) |
| 강의·프로그램 신청 | `public.applications` | 프로그램, 이름, 연락처, 신청 내용 |
| 게시글 | `public.project_posts` | 제목, 본문, 작성자 |

GitHub Pages(홈페이지 파일)에는 개인정보가 전혀 저장되지 않으며, 모든 통신은 HTTPS로 암호화됩니다.
`assets/config.js`에 공개된 **anon key는 공개용 키**로, 이 키로는 아래 RLS(행 수준 보안) 정책이
허락한 것만 할 수 있습니다. 절대 공개하면 안 되는 키는 **service_role key**이며,
이 키는 어떤 파일·코드에도 넣지 않습니다(§4 참고).

---

## 2. 코드에 이미 반영된 조치 (이번 커밋)

- **비밀번호 정책 강화** : 최소 6자 → **8자 이상 + 영문·숫자 조합 필수**, 비밀번호 확인 입력 추가
- **개인정보 수집·이용 동의** : 회원가입·신청 폼에 동의 체크박스 추가(미동의 시 가입 불가) — 개인정보 보호법상 필수
- **개인정보처리방침 페이지 신설** : `privacy.html` (수집 항목·목적·보유기간·파기·권리·책임자) + 푸터 링크
- **오류 메시지 한국어화** : 연결 실패·중복 가입·인증 미완료 등을 알아볼 수 있는 안내로 표시
- **신청자 연락처 이중 보호** : 일반 회원은 연락처가 아예 제외되고 이름이 가려진(홍*동) 조회 전용 뷰만 읽음
  (기존에는 화면에서만 가리고 DB에서는 읽을 수 있었음 → 개발자 도구로 열람 가능했던 허점 제거)
- **XSS 방어 유지** : 게시글·신청 내용 출력 시 HTML 이스케이프 처리(기존 코드 확인 완료)

---

## 3. SQL 보안 패치 — Supabase에서 1회 실행 (필수)

Supabase → **SQL Editor** → New query 에 **`supabase/security_patch.sql` 전체를 복사해 Run** 합니다.
여러 번 실행해도 안전합니다. 적용되는 내용은 다음과 같습니다.

1. **신청자 개인정보 접근 차단** — 원본 `applications` 테이블은 관리자만 조회.
   일반 회원은 연락처·메시지가 빠지고 이름이 가려진 `applications_board` 뷰만 조회.
2. **관리자 판별 함수 보안 강화** — `search_path` 고정(스키마 바꿔치기 공격 차단, Supabase 공식 권고)
3. **입력 길이 제한** — 이름 50자, 연락처 20자, 게시글 2만 자 등 (도배·초대용량 입력 차단)
4. **게시글 도배 방지** — 회원 1명당 5분에 3건까지만 작성 허용
5. **권한 변경 감사 기록** — 누가 언제 누구를 관리자로 올렸는지 `role_audit` 테이블에 자동 기록
6. **파기 정책** — 관리자가 보유기간 지난 신청 데이터를 삭제할 수 있는 권한

> 기존 설치 시 이미 적용된 것: RLS 전 테이블 활성화, 가입 시 role='member' 강제,
> 일반 회원의 관리자 자가 승격 차단(`protect_role` 트리거), 프로필은 본인·관리자만 열람.

---

## 4. Supabase 대시보드 설정 체크리스트 (클릭으로 5분)

대시보드 → 꿈들 프로젝트에서 아래를 하나씩 확인합니다.

| 항목 | 위치 | 권장 설정 | 이유 |
|---|---|---|---|
| 이메일 인증 | Authentication → Sign In / Up → Email → **Confirm email** | **켜기** | 남의 이메일로 위장 가입 차단 |
| 비밀번호 최소 길이 | Authentication → Sign In / Up → **Minimum password length** | **8** | 서버 차원에서도 8자 강제 |
| 비밀번호 문자 조건 | 같은 화면 → Password Requirements | **Letters and digits** | 영문+숫자 조합 서버 강제 |
| 요청 속도 제한 | Authentication → **Rate Limits** | 기본값 유지 확인 | 무차별 대입(브루트포스) 방어 |
| 봇 차단(선택) | Authentication → **Attack Protection → CAPTCHA** | Cloudflare Turnstile 연동 | 자동 가입 봇 차단(무료) |
| 유출 비밀번호 차단(선택·유료) | Attack Protection → Leaked password protection | Pro 요금제 시 켜기 | 이미 유출된 비밀번호 사용 차단 |
| **Supabase 계정 2단계 인증** | 대시보드 우상단 프로필 → Account → **MFA** | **반드시 켜기** | 대시보드가 뚫리면 모든 방어가 무의미 — 가장 중요 |
| 백업 확인 | Database → **Backups** | 무료: 자동 백업 없음 → §5의 수동 백업 수행 | 실수 삭제·사고 대비 |

**절대 금지 사항**
- **service_role key**를 `config.js`·GitHub·카카오톡 등 어디에도 붙여넣지 않습니다.
  (이 키는 모든 보안 정책을 무시하는 마스터키입니다. 유출 의심 시 Settings → API → **Reset** 즉시 실행)
- Supabase 대시보드 비밀번호를 다른 사이트와 같은 것으로 쓰지 않습니다.

---

## 5. 운영 수칙 (사람이 지키는 보안)

기술 조치보다 계정 관리가 뚫리는 경우가 훨씬 많습니다. 다음 수칙을 권합니다.

1. **관리자 계정은 최소한으로** — 관리자(role='admin')는 실제 필요한 1~2명만 둡니다.
   admin.html의 권한 변경은 신중히 하고, 분기마다 SQL Editor에서 점검합니다.
   ```sql
   select email, role, created_at from public.profiles where role = 'admin';
   ```
2. **월 1회 데이터 백업** — SQL Editor에서 아래를 실행해 CSV로 내려받아,
   암호를 건 개인 저장소(구글드라이브 등)에 보관합니다.
   ```sql
   select * from public.profiles;
   select * from public.applications;
   select * from public.project_posts;
   ```
3. **보유기간 지킨 파기** — 프로그램이 끝난 신청 데이터는 1년 이내에 삭제합니다(개인정보처리방침 약속).
   ```sql
   delete from public.applications where created_at < now() - interval '1 year';
   ```
4. **탈퇴 요청 처리** — 회원이 탈퇴를 원하면 대시보드 → Authentication → Users에서 해당 사용자를
   Delete 합니다. 프로필은 자동으로 함께 삭제(cascade)됩니다.
5. **주 1회 접속** — 무료 프로젝트 일시중지를 막기 위해 admin.html 또는 대시보드에 주 1회 접속합니다.
6. **공용 컴퓨터에서 관리자 로그인 금지** — 학교 공용 PC 등에서 로그인했다면 반드시 로그아웃합니다.

---

## 6. 만약 사고가 나면 (침해 대응 절차)

1. **즉시 차단** — Supabase Settings → API에서 anon key **Reset** → 새 키를 `assets/config.js`에 반영해 커밋.
   대시보드 비밀번호도 즉시 변경합니다.
2. **피해 파악** — `role_audit` 테이블과 Authentication → Users 목록에서 이상 계정·권한 변경을 확인합니다.
3. **이상 계정 조치** — 모르는 admin 계정은 즉시 member로 내리고, 악성 계정은 삭제합니다.
4. **법적 의무** — 개인정보 유출이 확인되면 「개인정보 보호법」에 따라
   **72시간 이내** 이용자 통지 및 개인정보보호위원회(privacy.go.kr) 신고 의무가 있습니다.
   유출 항목·시점·경위·조치를 기록으로 남깁니다.
5. **재발 방지** — 원인을 확인한 뒤 이 문서의 체크리스트를 처음부터 다시 점검합니다.

---

## 7. 한눈에 보는 완료 체크리스트

- [ ] Supabase 프로젝트 Restore (Paused 상태였다면) → 회원가입 정상 동작 확인
- [ ] `supabase/security_patch.sql` 을 SQL Editor에서 1회 실행
- [ ] Confirm email 켜기 · 비밀번호 최소 8자 + 영문/숫자 설정
- [ ] Supabase 계정에 2단계 인증(MFA) 켜기
- [ ] 관리자 계정 목록 점검 (필요한 사람만 admin)
- [ ] 월 1회 백업 · 연 1회 신청 데이터 파기 일정을 달력에 등록
